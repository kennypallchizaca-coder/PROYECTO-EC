/**
 * Archivo reservado para pruebas unitarias del componente Button.
 * Se provee un marcador para evitar errores de compilación mientras se define la estrategia de testing.
 */
export const buttonTestPlaceholder = true;
